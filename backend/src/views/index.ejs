<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page %></title>
</head>
<body>
    <% contracts.forEach(contract => { %>
        <p><%= contract.id %> - <%= contract.origen %> - <%= contract.estado %> - <%= contract.cupon %></p>
    <% }) %>
</body>
</html>